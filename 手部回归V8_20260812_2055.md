# 手部追踪回归 V8(稳定简单版)

日期: 2026-08-12 20:55
主文件: head_tracker_v12.py (2534 行 → 2254 行, 删减 280 行补丁)

## 背景
当前部署版(15:30, git HEAD 2a6f920)手部追踪叠加了 20+ 个补丁, 导致:
- **更抖**: 框在"完全预测/半跟随/正常更新"间多分支切换
- **方框分裂**: MediaPipe 双源检测 + 低分升级建轨 + 去重改窄 → 同手多轨
- **漂移**: merge_box/手肘防护/互斥链/高速豁免 → 错误检测喂入轨道

## 改动内容(核心)
以 08-12 01:47 备份 `head_tracker_v12.py.bak_0812_0147_完全沿用V8手` 的手部段为基线, 整段替换当前复杂手部段。

### 手部段恢复 V8 逻辑(1528-1679 行, 152 行)
| 环节 | V8(现) | 之前复杂版 |
|---|---|---|
| 检测源 | hand.pt 单源 | hand.pt + MediaPipe 验证器 + MediaPipe 补充通道 |
| 全局 conf | 0.12 | 0.08(过灵敏→弱框多→抖) |
| 低分池 | conf 0.12-0.25 只进池 | + 连续2帧升级建轨(分裂源) |
| 肤色验证 | conf<0.60 内联验证 | 抽函数复用(行为同) |
| 去重 | IoU>0.3 或 中心距<1.2手宽 | 仅 IoU>0.3(双手靠近不合并→分裂) |
| 匹配 | best_score = IoU + 外观×0.8 | 全局排序+预测优先+距离惩罚+惯性+距离得分+高速豁免 |
| 更新 | 直接 update(dh) | merge_box 位置-尺寸分离 + 手肘防护 + 手摸头防护 + hysteresis |
| 低分池维持 | 简单 IoU>0.20 | + 互斥 + 中心距验证 + 高速豁免 |
| 局部找回 | conf 0.15, imgsz 480 | conf 0.25 + 中心距验证 + 互斥 + 高速豁免 |
| 建轨抑制 | 固定 1.5 手宽 + 顺手喂 | + 取消手间距离限制 + 顺手防合并框 |
| 丢失容忍 | lost<5 | lost<5(同) |

### 其他调整
- MediaPipe 线程停用 (`if False and MP_HANDS is not None`, 代码保留, 可随时恢复)
- 删除 `_prev_low_pool` 低分升级机制(分裂源)
- 头部逻辑**未动**(本就已是"回归V8精神": 纯IoU严格匹配 + 预测一致性拦截 0.4头宽)
- KalmanBox 层**保留增强**(尺寸EMA治大小抖、丢失外推约束治乱飘、方向突变修正)—— 与 V8 精神一致且独立无副作用

## 备份
- `head_tracker_v12.py.bak_0812_1955_当前复杂版` — 修改前(若想回退用这个)
- `head_tracker_v12.py.bak_0812_0147_完全沿用V8手` — V8 手部基线

## 验证
- py_compile 通过
- 手部段与 V8 基线逐行一致 (152/152)
- KalmanBox 属性兼容 (sig/sig_pool/last_good/_size_ema/pn 等全部存在)
- 主循环全链路完整: 头检测→手检测→手追踪→物品检测→显示→Web(5050)

## 测试建议
1. 运行 `启动V32头部追踪.bat` 或直接 python head_tracker_v12.py
2. 重点观察: 手快速挥动、两手靠近/交叉、手遮挡后重现、摸头时手框
3. 如仍有具体问题, 告诉我现象(哪只手/什么动作/框怎么飘), 再针对性微调
